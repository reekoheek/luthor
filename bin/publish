#!/bin/bash

rsync -avz -e "ssh -p 1022" \
    --exclude \.git \
    --exclude \.gitignore \
    --exclude \.sass-cache \
    --exclude \bower_components \
    --exclude \composer.json \
    --exclude \composer.lock \
    --exclude \node_modules \
    --exclude \db \
    --exclude \assets \
    --exclude \README.md \
    --exclude \test \
    --exclude \project.sublime-project \
    --exclude \project.sublime-workspace \
    --exclude \package.json \
    --exclude \config.rb \
    --exclude \bower.json \
    --exclude \phpunit.xml \
    ./ xinix@xinix.co.id:/var/domains/luthor.dev.xinix.co.id/